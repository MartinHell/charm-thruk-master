<Component Thruk::Backend>
{% for unit in thruk_agent -%}
    <peer>
        name    = {{unit['nagios_context']}}
        id      = {{unit['thruk_id']}}
        type    = http
        section = {{unit['nagios_context']}}
        <options>
            peer          = {{unit['url']}}
            auth          = {{unit['thruk_key']}}
        </options>
    </peer>
{% endfor -%}
</Component>

